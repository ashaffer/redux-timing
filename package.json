{
  "name": "redux-timing",
  "description": "Timing functions (throttle/debounce) for redux actions",
  "repository": "git://github.com/ashaffer/redux-timing.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/debounce": "^1.0.5",
    "@f/throttle": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-preset-es2015": "^6.1.2",
    "babel-tape-runner": "^2.0.1",
    "tape": "^4.2.0"
  }
}
